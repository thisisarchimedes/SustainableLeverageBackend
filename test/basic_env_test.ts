import { expect } from 'chai';

describe('Basic Test Suite', () => {
  it('should pass this test if basic environment is configured correctly', () => {
    const number = 1;
    expect(number).to.equal(1);
  });
});
